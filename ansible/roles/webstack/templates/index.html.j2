<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>{{ site_title }} · {{ inventory_hostname }}</title>
    <style>
      :root {
        --accent: {{ accent_color }};
      }
      body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background: #f7f9ff;
        margin: 0;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
        color: #222;
      }
      header {
        background: var(--accent);
        color: white;
        padding: 2rem;
        text-align: center;
        box-shadow: 0 4px 16px rgba(0,0,0,0.2);
      }
      main {
        flex: 1;
        padding: 3rem;
        max-width: 960px;
        margin: auto;
      }
      section {
        background: white;
        border-radius: 16px;
        padding: 2rem;
        box-shadow: 0 8px 32px rgba(81, 56, 255, 0.1);
      }
      footer {
        text-align: center;
        padding: 1rem;
        font-size: 0.9rem;
        background: #eef1ff;
      }
      .hostname {
        font-weight: bold;
      }
    </style>
  </head>
  <body>
    <header>
      <h1>{{ site_title }}</h1>
      <p>{{ site_subtitle }}</p>
    </header>
    <main>
      <section>
        <p class="hostname">Nodo: {{ inventory_hostname }} · IP {{ ansible_host | default(ansible_default_ipv4.address) }}</p>
        {% for paragraph in content_paragraphs %}
        <p>{{ paragraph }}</p>
        {% endfor %}
        <p>Última configuración: {{ ansible_date_time.iso8601 }}</p>
      </section>
    </main>
    <footer>
      {{ footer_message }}
    </footer>
  </body>
</html>
